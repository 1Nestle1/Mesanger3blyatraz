import express from "express";
import prism from "../db";

const router = express.Router();

router.post("/send",async function name(params) {
    
})
router.post("/recive", async function name(params) {
    
})
router.post("/delete",async function name(params) {
    
})
export default router;